<Window x:Class="WarehouseAccountingApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Система складского учета" Height="720" Width="1180"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource RootBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SurfaceBackgroundBrush}" Padding="12"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Склад" FontSize="24" FontWeight="SemiBold" Margin="4,8,0,0"/>
                    <TextBlock Text="учет материалов" Style="{StaticResource MutedTextStyle}" Margin="4,0,0,18"/>

                    <Button x:Name="DashboardButton" Content="Главная" Style="{StaticResource NavButtonStyle}" Click="Dashboard_Click"/>
                    <Button x:Name="ProductsButton" Content="Товары" Style="{StaticResource NavButtonStyle}" Click="Products_Click"/>
                    <Button x:Name="SuppliersButton" Content="Поставщики" Style="{StaticResource NavButtonStyle}" Click="Suppliers_Click"/>
                    <Button x:Name="OperationsButton" Content="Приход / расход" Style="{StaticResource NavButtonStyle}" Click="Operations_Click"/>
                    <Button x:Name="HistoryButton" Content="История" Style="{StaticResource NavButtonStyle}" Click="History_Click"/>
                    <Button x:Name="ReportsButton" Content="Отчеты" Style="{StaticResource NavButtonStyle}" Click="Reports_Click"/>
                    <Button x:Name="UsersButton" Content="Пользователи" Style="{StaticResource NavButtonStyle}" Click="Users_Click"/>
                </StackPanel>

                <Button DockPanel.Dock="Bottom" Content="Выход" Click="Logout_Click"/>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Background="{StaticResource RootBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SurfaceBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="PageTitleText" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="UserTextBlock" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,10,0"/>
                        <Border Width="28" Height="28" CornerRadius="14" Background="{StaticResource BlueSurfaceBrush}">
                            <TextBlock x:Name="UserInitialsText" HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="{StaticResource BlueTextBrush}" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Padding="20">
                <ContentControl x:Name="MainContent"/>
            </Border>
        </Grid>
    </Grid>
</Window>
